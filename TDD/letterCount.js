class LetterCount {
    count(word) {
        const object = {};
        let count = 0;
        for (let i = 0; i < word.length; i++) {
            const char = word[i];
            if (char == word[i+1]) {
                
            }
        }
        return {'c': 1, 'a': 1, 't': 1};
    }
}

module.exports = LetterCount;